# PostgreSQL Database Settings
# These are used by the 'db' service in docker-compose to initialize the database.
POSTGRES_DB=mafia_comm_service
POSTGRES_USER=user
POSTGRES_PASSWORD=your_strong_password

# Application Settings
# This is the connection string your .NET app will use to connect to the database.
ConnectionStrings__DefaultConnection="Host=db;Port=5432;Database=mafia_comm_service;Username=user;Password=your_strong_password"